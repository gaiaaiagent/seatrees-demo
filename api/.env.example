DATABASE_URL=postgresql://postgres:password@localhost:5432/seatrees_demo
FRONTEND_URL=http://localhost:3000
PORT=4000
# Regen Ledger API — used by Fastify routes for live chain queries
# The Next.js API routes query polkachu directly and don't need this
REGEN_LEDGER_API=https://regen-api.polkachu.com
